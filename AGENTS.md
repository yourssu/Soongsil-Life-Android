# [슬기로운 숭실생활]
### 최소한의 코드 수정으로 작업해야 하며 코드 자체의 알고리즘이나 성능보다는 가독성을 우선합니다.
> 비밀(.env, api 키 등)은 절대 커밋하지 않는다.

> 모든 작업은 main 브랜치에 직접 푸시하지 않는다. 별도 브랜치에서 작업한다.

> 모든 비밀은 local.properties에서 관리한다.
- 작업 간 주석은 반드시 작성하며 한국어로 작성한다.
```
// 더하기를 하는 함수입니다.
// @param ...
fun add(a: Int, b: Int) {
    return a + b // a와 b를 더한 값을 리턴합니다.
}
```

- 데이터 클래스는 data 모듈에서 관리한다.
- 항상 사용자에게 수정 계획을 먼저 설명한다.
- Compose UI 작업을 하면 상황별 @Preview를 작성해야 한다.
  - 다크모드, 라이트모드도 대응되도록 해야한다.
    - 필요한 컬러시스템은 이미 Color.kt, Theme.kt에 정의되어 있다.